import java.util.Scanner;

public class TaskCapture {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        System.out.print("Enter task description: ");
        String taskDescription = scanner.nextLine();
        if (taskDescription.length() <= 50) {
            System.out.println("Task successfully captured");
        } else {
            System.out.println("Please enter a task description of less than 50 characters");
        }
    }
public class TaskIDTest {
    public static void main(String[] args) {
        String[] taskIDs = {"AD:1:BYN", "CR:0:IKE", "CR:1:ARD", "CR:2:THA", "CR:3:ND"};
        for (String taskID : taskIDs) {
            if (isValidTaskID(taskID)) {
                System.out.println(taskID + " is valid");
            } else {
                System.out.println(taskID + " is invalid");
            }
        }
    }

    public static boolean isValidTaskID(String taskID) {
        String[] parts = taskID.split(":");
        if (parts.length != 3) {
            return false;
        }
        if (!parts[0].equals("AD") && !parts[0].equals("CR")) {
            return false;
        }
        try {
            Integer.parseInt(parts[1]);
        } catch (NumberFormatException e) {
            return false;
        }
        return true;
    }
Here's a Java program that calculates the total hours for a given set of tasks and durations:

```
public class TaskDuration {
    public static void main(String[] args) {
        int[] durations1 = {10, 12, 55, 11, 1};
        int totalHours1 = calculateTotalHours(durations1);
        System.out.println("Total hours for task set 1: " + totalHours1);

        int[] durations2 = {8, 4, 16, 32, 28, 14};
        int totalHours2 = calculateTotalHours(durations2);
        System.out.println("Total hours for task set 2: " + totalHours2);
    }

    public static int calculateTotalHours(int[] durations) {
        int totalHours = 0;
        for (int duration : durations) {
            totalHours += duration;
        }
        return totalHours;
    }
}

 
